{
  "name": "stagein",
  "version": "0.2.0",
  "private": true,
  "description": "StageIn — Join the performance. Shape the moment.",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "node scripts/dev.mjs",
    "smoke": "node scripts/smoke.mjs",
    "ui-check": "node scripts/ui-check.mjs"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
